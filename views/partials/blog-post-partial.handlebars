<div class="card" style="width: 18rem;">
    <div class="card-body">
        <button
            type="button"
            class="btn-close delete-button"
            aria-label="delete"
            data-id={{blog_id_pk}}
            data-user-created={{user_id_fk}} 
        ></button>
        <h5 class="card-title">{{title}}</h5>
        <p class="card-text">{{post}}</p>
    </div>
</div>